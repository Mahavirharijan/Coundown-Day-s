# TODO: Modify reminder.yml Workflow

- [x] Add `if: github.event.inputs.mode == 'unlock'` condition to the `unlock-bot` job in `.github/workflows/reminder.yml`
- [x] Add `if: github.event.inputs.mode == 'reminder'` condition to the `reminder-bot` job in `.github/workflows/reminder.yml`
